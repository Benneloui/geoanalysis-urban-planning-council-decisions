# Core Dependencies
requests>=2.31.0
pandas>=2.1.0
pyarrow>=14.0.0
pyyaml>=6.0

# RDF/Linked Data
rdflib>=7.0.0

# Geocoding
geopy>=2.4.0

# PDF Processing
PyMuPDF>=1.23.0  # fitz
pdfplumber>=0.10.0

# NLP (Location Extraction)
spacy>=3.7.0
thefuzz>=0.20.0
python-Levenshtein>=0.23.0

# OCR (Optional - for scanned PDFs)
# Requires: tesseract-ocr system package
# macOS: brew install tesseract
# Ubuntu: sudo apt-get install tesseract-ocr tesseract-ocr-deu
pytesseract>=0.3.10
Pillow>=10.0.0

# HTTP Retry Logic
urllib3>=2.0.0

# Development/Analysis (Optional)
jupyter>=1.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
folium>=0.15.0
geopandas>=0.14.0  # For shapefile export
openpyxl>=3.1.0  # For Excel export

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0  # Parallel test execution
pytest-timeout>=2.2.0  # Test timeouts
pytest-mock>=3.12.0  # Mocking utilities

# Validation
pyshacl>=0.25.0  # SHACL RDF validation (optional)

# Enrichment (Optional)
transformers>=4.36.0  # For sentiment analysis (optional, large dependency)
torch>=2.1.0  # Required for transformers (optional, large dependency)
